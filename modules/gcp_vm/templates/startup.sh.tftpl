#!/usr/bin/env bash
set -euxo pipefail

# Provider-specific startup wrapper.
# In this scaffold, the stack passes `startup_script` directly to the instance.
# Keep this template available for future provider-specific extensions.
